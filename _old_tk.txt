/* ============================================================
   티카투카 (주사위 대결)
   ============================================================ */
.tk-head {
  display: flex;
  align-items: center;
  gap: 0.8rem;
  flex-wrap: wrap;
  margin-bottom: 1rem;
  font-size: 0.9rem;
}
.tk-round { font-weight: 700; font-size: 1.05rem; }
.tk-record { color: var(--muted); }
.tk-level {
  margin-left: auto;
  color: var(--warn);
  border: 1px solid rgba(240, 176, 114, 0.35);
  border-radius: 999px;
  padding: 0.1rem 0.6rem;
  font-size: 0.78rem;
}

.tk-side { margin: 0.2rem 0; }
.tk-side-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.8rem;
  padding: 0.3rem 0.1rem;
  font-size: 0.85rem;
}
.tk-who { font-weight: 600; }
.tk-who.npc { color: var(--accent-2); }
.tk-who.me { color: var(--accent); }
.tk-specials { color: var(--muted); font-size: 0.8rem; }
.tk-none { opacity: 0.6; }

.tk-board {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.5rem;
}

.tk-col {
  display: grid;
  gap: 0.35rem;
  padding: 0.35rem;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: #10151c;
  font: inherit;
  cursor: default;
  transition: border-color 0.15s ease, background 0.15s ease;
}
.tk-col.playable { cursor: pointer; }
.tk-col.playable:hover { border-color: var(--accent); background: rgba(94, 234, 212, 0.07); }

.tk-cell {
  display: grid;
  place-items: center;
  height: clamp(38px, 11vw, 52px);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.03);
}

.die {
  display: grid;
  place-items: center;
  width: clamp(30px, 9vw, 40px);
  height: clamp(30px, 9vw, 40px);
  border-radius: 8px;
  background: linear-gradient(160deg, #f4f7fa, #cfd6df);
  color: #10151c;
  font-weight: 700;
  font-size: clamp(0.95rem, 3.4vw, 1.15rem);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.45);
  border: 2px solid transparent;
}
/* 특수 주사위 — 테두리를 진하게 */
.die.special {
  border-color: var(--accent);
  box-shadow: 0 0 0 1px var(--accent), 0 0 10px rgba(94, 234, 212, 0.55);
  background: linear-gradient(160deg, #ffffff, #bfe9e1);
}
.die.big { width: 54px; height: 54px; font-size: 1.5rem; border-radius: 10px; }
.die.mini {
  width: 20px; height: 20px; font-size: 0.7rem;
  border-radius: 5px; display: inline-grid; vertical-align: -4px;
}

.tk-sums {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.5rem;
  margin: 0.35rem 0;
}
.tk-sum {
  text-align: center;
  font-variant-numeric: tabular-nums;
  font-weight: 700;
  font-size: 0.95rem;
  color: var(--text);
}

.tk-versus {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.5rem;
  margin: 0.5rem 0;
}
.tk-line {
  text-align: center;
  font-size: 0.8rem;
  padding: 0.2rem 0;
  border-radius: 999px;
  border: 1px solid var(--line);
  color: var(--muted);
}
.tk-line.win { color: var(--accent); border-color: rgba(94, 234, 212, 0.4); }
.tk-line.lose { color: var(--accent-2); border-color: rgba(167, 139, 250, 0.4); }

.tk-hand {
  display: flex;
  align-items: center;
  gap: 1rem;
  flex-wrap: wrap;
  margin-top: 1.1rem;
  padding-top: 1rem;
  border-top: 1px solid var(--line);
}
.tk-die-slot { display: flex; align-items: center; gap: 0.6rem; }
.tk-controls { display: flex; gap: 0.5rem; flex-wrap: wrap; margin-left: auto; }

.status.warn { color: var(--warn); }

.tk-rules { margin: 0.4rem 0 0; padding-left: 1.1rem; font-size: 0.9rem; color: var(--muted); }
.tk-rules li { margin-bottom: 0.5rem; }
.tk-rules b { color: var(--text); }
.tk-rules .ex {
  display: block;
  color: var(--accent);
  font-size: 0.82rem;
  margin-top: 0.15rem;
}

