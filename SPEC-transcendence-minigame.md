# 석판 부수기 — 웹 구현 스펙

> 로스트아크 '장비 초월'(2024/12/18 개편 이전) 미니게임의 규칙을 참고해 만드는
> 웹 기반 퍼즐 게임. 원작의 이미지·사운드·폰트 등 게임 에셋은 일절 사용하지 않고,
> 규칙만 참고해 자체 아트로 구현한다.

---

## 0. 이 문서를 읽는 에이전트에게

- 이 문서는 **완성된 요구사항**이다. 규칙에 없는 부분은 임의로 만들지 말고 `TODO(확인)` 주석을 남긴 뒤 진행할 것.
- 아래 `§9 불확실 항목`에 표시된 수치는 원작에서 정확히 확인되지 않은 값이다. **전부 설정 파일로 빼서** 나중에 튜닝할 수 있게 만든다.
- 게임 로직과 렌더링을 반드시 분리한다. 로직은 순수 함수 + 주입된 RNG로 작성해 단위 테스트가 가능해야 한다.
- 솔버(최적 수 추천)는 **이번 범위 밖**이다. 다만 나중에 붙일 수 있도록 `applyMove(state, move, rng) -> state` 형태의 순수 함수 경계를 지킬 것.

---

## 1. 목표와 범위

### 만드는 것
- 브라우저에서 혼자 플레이할 수 있는 격자 퍼즐 게임 1개
- 난이도 프리셋(원작의 '단계'에 해당)을 **차례로 개방**하며 진행 → 성공/실패 판정 → 재시작
  - 처음에는 1단계만 고를 수 있다. 어떤 단계를 **한 번이라도 클리어하면 다음 단계가 열린다.**
  - 한 번 열린 단계는 다시 잠기지 않는다. 개방 여부는 로컬 통계의 단계별 클리어 횟수로 판단한다.
- 로컬 통계(플레이 횟수, 성공률, 등급 분포) 저장

### 안 만드는 것
- 서버, 계정, 랭킹, 멀티플레이
- 최적 수 추천 솔버
- 원작 재화·장비 시뮬레이션(골드, 재료 수급 등)

---

## 2. 기술 스택

| 항목 | 선택 | 이유 |
| --- | --- | --- |
| 빌드 | Vite | 설정 최소, 정적 배포 |
| 언어 | TypeScript (strict) | 상태 타입이 복잡해서 필수 |
| UI | React 18 | 격자 렌더링과 상태 표현에 무난 |
| 상태 | Zustand 또는 useReducer | 전역 스토어 하나면 충분, Redux는 과함 |
| 스타일 | CSS Modules 또는 Tailwind | 취향껏 |
| 테스트 | Vitest | 로직 단위 테스트 필수 |
| 배포 | 정적 호스팅(Vercel / GitHub Pages) | 서버 없음 |

**중요**: 게임 로직은 `src/core/` 아래에 React 의존성 없이 순수 TS로 작성한다. UI는 `src/ui/`.

---

## 3. 도메인 모델

```ts
// src/core/types.ts

/** 격자 좌표. 왼쪽 위가 (0,0), x=열, y=행 */
export interface Pos { x: number; y: number; }

export type TileKind =
  | 'empty'      // 파괴됐거나 처음부터 비어 있는 칸
  | 'blocked'    // 보드 밖(비활성 영역). 석판이 재생성될 수 없다
  | 'normal'     // 고대 석판 — 파괴 대상
  | 'distorted'; // 왜곡된 고대 석판 — 파괴 대상이 아님

/** 정령이 머무른 석판에 깃든 효과. normal 타일에만 부여된다 */
export type Blessing =
  | 'reshuffle'  // 재배치
  | 'blessing'   // 축복
  | 'extra'      // 추가
  | 'mystery'    // 신비
  | 'upgrade'    // 강화
  | 'clone';     // 복제

export interface Tile {
  kind: TileKind;
  blessing?: Blessing; // kind === 'normal' 일 때만 존재
}

export type Board = Tile[][]; // [y][x]

export type SpiritId =
  | 'blaze'      // 업화
  | 'bigBang'    // 대폭발
  | 'bolt'       // 벼락
  | 'strike'     // 낙뢰
  | 'twister'    // 용오름
  | 'shockwave'  // 충격파
  | 'quake'      // 지진
  | 'tsunami'    // 해일
  | 'storm'      // 폭풍우
  | 'purify'     // 정화
  | 'erupt'      // 분출      (신비 전용)
  | 'resonance'; // 세계수의 공명 (신비 전용)

export interface SpiritCard {
  id: SpiritId;
  level: 1 | 2 | 3;
}

export interface GameState {
  board: Board;
  /** 지금 사용할 수 있는 정령 2장 */
  hand: [SpiritCard, SpiritCard];
  /** 다음에 들어올 정령 미리보기 큐 */
  queue: SpiritCard[];
  /** 남은 정령 소환 횟수 */
  chargesLeft: number;
  /** 남은 정령 교체(리롤) 횟수 */
  rerollsLeft: number;
  /** 지금까지 소모한 소환 횟수 */
  chargesUsed: number;
  status: 'playing' | 'cleared' | 'failed';
  /** cleared 일 때만 채워짐 */
  grade?: 1 | 2 | 3;
  stage: StageConfig;
  log: MoveLogEntry[];
}
```

---

## 4. 게임 규칙

### 4.1 승패 조건

- **성공**: 보드에서 `normal` 타일이 0개가 되면 즉시 클리어. `distorted` 타일은 남아 있어도 된다.
- **실패**: `chargesLeft`가 0이 됐는데 `normal` 타일이 남아 있으면 실패.
- **등급**: 클리어 시점의 `chargesUsed`로 결정한다.

```
chargesUsed <= stage.gradeThresholds[3] → 3성
chargesUsed <= stage.gradeThresholds[2] → 2성
그 외                                   → 1성
```

- 사용자는 언제든 **중단**할 수 있다(원작의 [초월 중단]). 중단하면 판이 리셋되고 통계에 '중단'으로 기록된다.

### 4.2 턴 진행

1. 플레이어가 손패 2장 중 하나를 고른다.
2. 보드의 한 칸을 클릭해 **시전 지점**을 정한다. 호버 시 타격 범위를 미리보기로 표시한다.
3. 해당 정령의 범위·확률에 따라 타일을 파괴한다(§4.3).
4. 파괴된 타일 중 `blessing`이 있던 타일의 효과를 **파괴 처리가 모두 끝난 뒤** 순서대로 적용한다(§4.5).
5. `chargesLeft`를 1 감소시킨다. 단 `blessing` 효과가 발동했다면 감소하지 않는다.
6. 사용한 손패 자리를 큐에서 채운다. 이때 손패 2장이 같은 정령이면 **강화**가 일어난다(§4.4).
7. 승패를 판정한다.

플레이어는 턴 중 언제든 **정령 교체**를 쓸 수 있다. **지금 고른 손패 1장만** 새로 뽑고 `rerollsLeft`를 1 감소시킨다. 나머지 한 장은 그대로 남으며, 새로 온 카드가 남은 카드와 같으면 평소처럼 합쳐진다. 소환 횟수는 소모되지 않는다.

### 4.3 파괴 판정

- **시전 지점의 타일은 확정 파괴**된다.
- 시전 지점을 제외한 범위 내 타일은 정령별 확률로 각각 독립 판정한다.
- 확률에는 두 가지가 있다.
  - **고정 확률** — 범위 안 어디든 같은 확률 (업화·낙뢰·충격파·용오름·정화)
  - **거리 감쇠** — 시전 지점 100% 에서 한 칸 멀어질 때마다 일정 비율씩 떨어지고
    하한 아래로는 내려가지 않는다. 멀리 뻗는 **지진·폭풍우·대폭발·해일**에 적용한다.
    거리는 체비쇼프로 잰다(가로·세로·대각 모두 한 칸 = 1).

    | 거리 | 0 | 1 | 2 | 3 | 4 | 5 이상 |
    | --- | --- | --- | --- | --- | --- | --- |
    | 확률 | 100% | 85% | 70% | 55% | 40% | 25% |

    감쇠 폭과 하한은 `src/data/config.ts` 의 `FALLOFF_STEP` · `FALLOFF_MIN` 이다.
- 범위가 보드 밖으로 나가는 부분은 그냥 무시한다(범위가 잘려도 페널티 없음).

**왜곡된 석판 처리**
- 기본적으로 `distorted` 타일은 파괴되지 않는다. 대신 **피격되면** `normal` 타일 **1~3개(균등 랜덤)**가 보드의 빈 칸(`empty`) 중 무작위 위치에 재생성된다.
  - 피격 = 파괴 범위에 포함되었고 파괴 판정을 통과한 경우.
  - 한 번의 시전에서 왜곡 석판 여러 개가 피격되면 **각각 따로 1~3을 뽑는다**. 둘을 때리면 2~6개가 된다.
  - 되살아날 자리는 빈 칸 중에서 고르되, **이번 시전으로 방금 부순 칸은 뒷순위로 민다.** 다른 빈 칸이 모자랄 때만 쓴다.
    애써 부순 자리가 그 자리에서 되살아나면 한 일이 없던 것처럼 보이기 때문이다.
  - 재생성될 빈 칸이 부족하면 가능한 만큼만 재생성한다.
  - 재생성된 타일에는 `blessing`이 붙지 않는다.
- `purify`(정화)와 `resonance`(세계수의 공명)는 왜곡 석판을 **파괴할 수 있고**, 이 피격으로는 재생성이 일어나지 않는다.

**정령 강화 단계별 파괴 규칙**

| 레벨 | 동작 |
| --- | --- |
| 1 | 시전 지점 확정 + 나머지는 `destroyChance` 확률 |
| 2 | **범위 내 모든 석판을 확정 파괴** (왜곡 석판도 확정 피격 → 재생성 유발) |
| 3 | **범위 내에서 왜곡 석판을 제외한 모든 석판을 확정 파괴** (왜곡은 건드리지 않음) |

> 2단계가 3단계보다 위험한 구간이 있다는 점이 이 게임의 재미 포인트다. 반드시 이대로 구현할 것.

- `purify`는 원래부터 왜곡 파괴가 가능하며, 3단계일 때 범위가 위아래 1칸씩 넓어진다.
- `bolt`(벼락)는 범위 개념이 없어 위 표를 따르지 않는다. §4.4 표의 특수 규칙을 따른다.
- `erupt`, `resonance`는 강화되지 않는다(항상 레벨 1 취급).

### 4.4 정령 데이터

`src/core/spirits.ts`에 데이터 테이블로 정의한다. 범위는 시전 지점 기준 상대 좌표 배열 또는 생성 함수로 표현한다.

| id | 이름 | 범위 | 1레벨 파괴 확률 | 드로우 확률 | 특수 |
| --- | --- | --- | --- | --- | --- |
| `blaze` | 업화 | 마름모, 맨해튼 거리 ≤ 2 | 50% | 11.5% | |
| `bigBang` | 대폭발 | X자 — 양 대각선 전체 | 거리 감쇠 | 10.5% | |
| `bolt` | 벼락 | 없음(위치 무관) | — | 9% | 아래 참조 |
| `strike` | 낙뢰 | 마름모, 맨해튼 거리 ≤ 1 | 50% | 15% | |
| `twister` | 용오름 | X자 — 대각선 ±1칸 | `TODO(확인)` | 15% | |
| `shockwave` | 충격파 | 3×3 정사각 | 75% | 9.5% | |
| `quake` | 지진 | 가로 한 줄 전체 | 거리 감쇠 | 7% | |
| `tsunami` | 해일 | 십자 — 가로+세로 한 줄 전체 | 거리 감쇠 | 5.5% | |
| `storm` | 폭풍우 | 세로 한 줄 전체 | 거리 감쇠 | 7% | |
| `purify` | 정화 | 가로 3칸(좌우 1칸). 3레벨 시 위아래 1칸 추가 | `TODO(확인)` | 10% | 왜곡 파괴 가능 |
| `erupt` | 분출 | 지정한 1칸만 | 100% | 신비 전용, 50% | 강화 불가 |
| `resonance` | 세계수의 공명 | 십자 넓은 범위 `TODO(확인)` | 100% | 신비 전용, 50% | 왜곡 파괴 가능, 강화 불가 |

**벼락(`bolt`) 특수 규칙**

범위 대신 **파괴 개수를 뽑는다.** 그중 한 개는 **시전 지점**이고, 나머지는 무작위다.

- 레벨 1: 1~3개 중 균등 랜덤
- 레벨 2: 1~5개 중 균등 랜덤
- 레벨 3: 1~7개 중 균등 랜덤
- **시전 지점이 `normal`이면 반드시 파괴된다.** 남은 개수만큼 보드의 다른 `normal` 타일 중 무작위로 골라 파괴한다.
- 시전 지점이 `distorted`면 벼락은 그 칸을 건드리지 않는다. 확정 파괴 없이 뽑힌 개수 전부를 무작위로 고른다. 피격도 재생성도 없다.
- `distorted`는 어떤 경우에도 고르지 않는다.
- 최소가 1이므로 "아무것도 부수지 못하는" 경우는 없다.

**강화 규칙**
- 손패 2장이 **같은 `id`**이면, 두 장이 합쳐져 한 장이 되고 빈 자리는 큐에서 채운다. **레벨이 같을 필요는 없다.**
  결과 레벨은 **둘 중 높은 쪽 + 1**이고 3에서 멈춘다.
  
  | 조합 | 결과 |
  | --- | --- |
  | Lv1 + Lv1 | Lv2 |
  | Lv2 + Lv1 | **Lv3** |
  | Lv2 + Lv2 | Lv3 |
  | Lv3 + 무엇이든 | 합쳐지지 않음 |
- 합치기는 **손패 안에서만** 일어난다. 큐에 있는 카드는 아직 손에 든 것이 아니므로 손패와 합쳐지지 않는다 — **손에 들어온 뒤에야 강화된다.** 큐가 내놓는 카드는 언제나 Lv1 이다.
  빈 자리를 큐에서 채운 뒤 다시 검사하므로, 채운 카드가 마침 같은 정령이면 그때 합쳐진다.
  3레벨은 같은 정령 석 장이면 나온다: `Lv1 + Lv1 → Lv2`, 그 `Lv2` 옆자리에 `Lv1` 이 들어오면 `Lv3` 이다.
- 최대 3레벨. 3레벨은 무엇과도 합쳐지지 않는다.
- `erupt`, `resonance`는 강화 대상이 아니다.

### 4.5 특수 석판(정령이 머무른 석판)

`normal` 타일에 `blessing`이 부여된다. 파괴되는 순간 효과가 발동한다.
한 시전으로 여러 개가 동시에 파괴되면 **`reshuffle` → 나머지** 순서로 처리한다.

**등장 방식 — 떠도는 특수 석판**

판 시작 시점에는 특수 석판이 **없다.** 플레이어가 첫 시전을 하고 난 뒤부터 생긴다.

- 매 시전이 끝날 때마다, 보드에 남은 `normal` 타일 중 **무작위 한 칸**이 특수 석판이 된다.
  효과 종류도 그때 무작위로 뽑는다.
- 보드에 동시에 존재하는 특수 석판은 **항상 `stage.blessingCount` 개**(기본 1개)다.
- 플레이어가 그 칸을 파괴하면 효과가 발동한다.
- 파괴하지 못하면 그 표시는 사라지고, **다른 무작위 칸이 다른 무작위 효과**로 바뀐다.
- 즉 특수 석판은 고정된 보물이 아니라 매 턴 자리와 효과가 바뀌는 **움직이는 표적**이다.
- 왜곡 석판과 재생성된 석판에는 부여되지 않는다.

| blessing | 이름 | 효과 |
| --- | --- | --- |
| `reshuffle` | 재배치 | 보드에 남은 모든 석판(`normal` + `distorted`)의 위치를 무작위로 재배치한다. 종류별 개수는 유지. `blessing`은 유지된 채 함께 이동한다 |
| `blessing` | 축복 | 이번 시전은 `chargesLeft`를 소모하지 않는다 |
| `extra` | 추가 | `rerollsLeft` +1 |
| `mystery` | 신비 | 손패에 남아 있는 정령 1장이 `erupt` 또는 `resonance`로 교체된다(각 50%) |
| `upgrade` | 강화 | 손패에 남아 있는 정령의 레벨 +1. 이미 3레벨이거나 신비 2종이면 효과 없음 |
| `clone` | 복제 | 손패에 남아 있는 정령이 방금 사용한 정령(레벨·신비 포함)과 동일한 카드로 교체된다 |

> "손패에 남아 있는 정령"은 이번 턴에 쓰지 않은 나머지 한 장을 뜻한다.

### 4.6 보드 프리셋

`src/data/stages.ts`에 난이도별 초기 배치를 하드코딩한다. 무작위 생성이 아니라 **고정 배치**다.
특수 석판은 초기 배치에 포함되지 않고 §4.5 의 떠도는 방식으로 매 턴 생성된다.

```ts
export interface StageConfig {
  id: string;              // 'stage-3' 등
  label: string;           // '3단계'
  width: number;
  height: number;
  /** 문자열 맵. '.' = blocked, 'O' = normal, 'X' = distorted */
  layout: string[];
  charges: number;         // 총 정령 소환 횟수
  rerolls: number;         // 정령 교체 횟수
  blessingCount: number;   // 보드에 동시에 존재하는 특수 석판 개수 (§4.5)
  gradeThresholds: { 3: number; 2: number };
}
```

예시 (`TODO(확인)` — 실제 원작 배치가 아니라 형식 예시다. 밸런싱은 플레이해보고 조정할 것):

```ts
{
  id: 'stage-1',
  label: '1단계',
  width: 8, height: 7,
  layout: [
    '..OOOO..',
    '.OOOOOO.',
    'OOOOOOOO',
    'OOOOOOOO',
    'OOOOOOOO',
    '.OOOOOO.',
    '..OOOO..',
  ],
  charges: 12,
  rerolls: 3,
  blessingCount: 5,
  gradeThresholds: { 3: 8, 2: 10 },
}
```

최소 5개 단계(쉬움 → 어려움)를 만든다. 단계가 올라갈수록 `distorted`가 늘고 `charges`가 빡빡해진다.

---

## 5. 파일 구조

```
src/
  core/
    types.ts          # 위 §3
    spirits.ts        # 정령 데이터 테이블 + 범위 계산 함수
    board.ts          # 보드 생성, 조회, 재생성/재배치 헬퍼
    engine.ts         # applyMove, reroll, checkStatus 등 순수 로직
    rng.ts            # 시드 가능한 RNG (mulberry32 등)
  data/
    stages.ts
  ui/
    App.tsx
    BoardView.tsx     # 격자 렌더링 + 범위 미리보기
    HandView.tsx      # 손패 2장 + 큐 미리보기
    StatusBar.tsx     # 남은 횟수, 교체 횟수, 등급 예상
    ResultModal.tsx
    StatsPanel.tsx
  store/
    gameStore.ts
```

### RNG는 반드시 주입한다

```ts
export interface Rng { next(): number; } // [0, 1)

export function applyMove(state: GameState, move: Move, rng: Rng): GameState;
```

`Math.random()`을 로직 안에서 직접 부르지 말 것. 시드 고정이 안 되면 테스트를 못 쓴다.

---

## 6. UI 요구사항

- **격자**: 타일은 정사각. `blocked`는 아예 렌더링하지 않거나 배경과 동일하게 처리해 보드 실루엣이 드러나게 한다.
- **범위 미리보기**: 정령을 선택한 상태로 타일에 호버하면 타격 범위를 하이라이트한다. 확정 파괴(시전 지점)와 확률 파괴를 **시각적으로 구분**한다. 범위 안에 `distorted`가 포함되면 경고색으로 표시한다 — 이 게임의 핵심 판단 정보다.
- **손패**: 2장을 크게, 다음 큐 3~4장을 작게. 강화 레벨은 카드 테두리나 배지로 표시한다.
- **모바일**: 호버가 없으므로 탭 1회 = 범위 미리보기, 탭 2회 = 시전으로 처리한다.
- **왜곡 경고**: 범위에 왜곡이 들어오면 경고를 띄우되, **확정으로 때리는지 확률로 때리는지 구분해서** 보여 준다.
  시전 지점이거나 레벨 2면 확정이고(칸 뱃지 `⚠확`), 범위 안에 걸쳐 있을 뿐이면 그 확률로만 맞는다(칸 뱃지 `⚠50%`).
  둘을 같은 표시로 뭉뚱그리면 피할 수 있는 수와 피할 수 없는 수를 구분할 수 없다.
- **애니메이션**: 결과를 이해시키는 모션만 남기고 장식 모션은 넣지 않는다. 대상은 **타일 파괴 · 왜곡 재생성 · 왜곡 피격** 셋이다.
  - 한 칸의 재생 시간은 짧게(150~300ms) 둔다.
  - 파괴는 **시전 지점에서 멀어질수록 늦게 시작한다**(칸당 30ms, 최대 240ms). 바깥으로 번져 나가 보이고, 덤으로 거리 감쇠(§4.3)가 눈으로 읽힌다. 범위가 없는 벼락은 거리 대신 순번으로 늦춘다.
  - **왜곡 피격**은 부서지지 않고 경고색으로 번쩍이며 판 전체가 한 번 흔들린다. 석판이 되살아나는 원인이라 반드시 눈에 띄어야 한다.
  - **재생성**은 파괴가 끝난 뒤 한 장씩 차례로 나타난다.
  - 파괴 모습은 정령 계열(`SpiritDef.motif`)에 따라 색과 방향만 달라진다 — 불(업화·분출), 번개(낙뢰·벼락), 폭발(충격파·대폭발), 바람(용오름·폭풍우), 땅(지진), 물(해일), 빛(정화·공명).
    이름과 어긋난 모습으로 터지면 무엇을 썼는지 판에서 읽히지 않기 때문이다. 규칙에는 영향을 주지 않는다.
  - 어느 칸이 부서지고 되살아나고 피격됐는지는 화면이 판을 비교해 추측하지 않고 `MoveLogEntry` 의 좌표 목록(`destroyed` / `regenerated` / `distortedHits`)을 그대로 쓴다. 비교로는 셋을 구분할 수 없고 왜곡 피격은 `kind` 가 그대로라 아예 잡히지 않는다.
- **접근성**: 키보드로 격자 이동 및 시전이 가능해야 하고, `prefers-reduced-motion`을 존중한다. 왜곡 석판을 색으로만 구분하지 말고 형태나 아이콘도 다르게 한다.

### 비주얼 방향

원작 에셋을 쓰지 않으므로 아트 디렉션을 직접 정해야 한다. "고대 유적의 석판을 정령의 힘으로 부순다"는 소재에서 출발하되, 다크 판타지 게임 UI의 기본값(검은 배경 + 보라 글로우 + 금색 테두리)으로 바로 가지 말 것. 석재의 질감, 균열이 번지는 방식, 조각이 떨어져 나가는 물리감 중 하나를 골라 그것만 밀어붙이고 나머지는 조용하게 둔다.

---

## 7. 구현 순서

1. **M1 — 로직 코어**: 타입, RNG, 보드 생성, 정령 범위 계산. 단위 테스트부터 작성.
2. **M2 — 엔진**: `applyMove` 완성. 왜곡 재생성, 강화 레벨별 분기, 벼락 특수 규칙, 특수 석판 6종.
3. **M3 — 최소 UI**: 격자 + 손패 + 클릭 시전. 스타일 없이 동작만.
4. **M4 — 판정과 흐름**: 승패, 등급, 중단, 재시작, 결과 모달, 단계 개방.
5. **M5 — UI 다듬기**: 범위 미리보기, 애니메이션, 모바일 대응, 아트 디렉션 적용.
6. **M6 — 통계**: localStorage에 플레이 기록 저장, 성공률·등급 분포 패널.

M2가 끝난 시점에 **CLI로 1000판 자동 플레이(무작위 수)를 돌려** 클리어율을 확인한다. 클리어율이 0%나 100%에 가까우면 `charges`나 `blessingCount`를 조정한다.

---

## 8. 테스트 케이스

로직 테스트는 시드 고정 RNG로 작성한다. 최소 아래는 반드시 포함한다.

- 3×3 보드 전체가 `normal`일 때 `shockwave` 레벨 2를 중앙에 쓰면 9칸 전부 파괴된다.
- 범위 안에 `distorted`가 있고 레벨 2로 시전하면, 왜곡이 피격되어 `normal` 1~3개가 재생성된다.
- 같은 상황에서 레벨 3으로 시전하면 왜곡은 피격되지 않고 재생성도 없다.
- `purify`로 `distorted`를 파괴하면 재생성이 일어나지 않는다.
- `bolt` 는 최소 1개를 부수며, 그 1개는 시전 지점이다.
- `blessing`(축복) 타일을 파괴한 턴은 `chargesLeft`가 줄지 않는다.
- 손패에 같은 정령 같은 레벨 2장이 모이면 1장의 상위 레벨로 합쳐진다.
- `normal`이 0이 되면 `distorted`가 남아 있어도 `status === 'cleared'`다.
- `chargesLeft`가 0이고 `normal`이 남으면 `status === 'failed'`다.
- `applyMove`가 입력 `state`를 변형하지 않는다(불변성).

---

## 9. 불확실 항목 — 전부 설정으로 뺄 것

원작에서 정확한 값이 확인되지 않은 항목들이다. 코드에 상수로 박지 말고 `src/data/config.ts`에 모아둔다.

- `blaze`, `strike` 외 정령들의 1레벨 파괴 확률 (충격파 75%, 업화·낙뢰 50%만 확인됨)
  - 지진·폭풍우·대폭발·해일은 고정 확률 대신 **거리 감쇠**(§4.3)를 쓴다. 감쇠 폭 15%p, 하한 25% 도 미확인 값이다.
  - 용오름·정화는 고정 50% 로 두었으나 확인된 값이 아니다.
- `bigBang`, `twister`, `resonance`의 정확한 범위 모양
- 단계별 보드 배치, 총 소환 횟수, 교체 횟수
- 등급 판정 기준선
- 특수 석판 6종의 부여 확률(현재는 균등 가정)
- 손패가 정확히 2장인지, 큐 미리보기가 몇 장인지

이 값들은 **밸런싱 대상**이지 원작 재현 대상이 아니다. 재미있는 쪽으로 조정하면 된다.

---

## 10. 지켜야 할 것

- 원작 게임의 이미지, 아이콘, 사운드, 폰트, UI 스크린샷을 복사해 쓰지 않는다. 아트는 전부 자체 제작하거나 라이선스가 명확한 것만 사용한다.
- 정령 이름은 한국어 그대로 써도 무방하나, 배포 시 원작과 무관한 별도 창작물임을 README에 명시한다.
- 원작 브랜드명이나 로고를 프로젝트 이름·도메인·파비콘에 쓰지 않는다.
